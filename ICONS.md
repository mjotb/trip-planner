# دليل الأيقونات والأنيميشن

كل موضع في التطبيق يشير إلى **خانة** لها اسم ثابت. لتغيير أي أيقونة، خطوتان فقط:

1. **سمِّ الملف باسم الخانة** — مثلًا `tab-trip.svg`
2. **ضعه في** `public/assets/custom/`

وانتهى. لا تعديل كود إطلاقًا. البناء يلتقط الملف تلقائيًا ويقرّر بنفسه أهي أيقونة ملوّنة أم تتبع لون السياق.

الملفات المقبولة: `.svg` (يُفضّل) · `.png` · `.webp`

أي خانة لا تضع لها ملفًا تبقى على أيقونتها الحالية، فيمكنك الاستبدال على دفعات بلا أن ينكسر شيء.

**للتأكد أن ملفاتك التُقطت** شغّل:

```bash
npm run icons
```

يطبع قائمة بما وجده ووضع عرض كل أيقونة، وينبّهك على أي اسم ملف لا يطابق خانة (مع سرد الأسماء الصحيحة).

---

## المواصفات

| البند | القيمة |
|---|---|
| الصيغة | SVG (يُفضّل)، أو PNG شفاف بضعف المقاس |
| مساحة الرسم | مربّعة — `viewBox="0 0 24 24"` |
| الحدود | حوّل الخطوط إلى مسارات (Outline Stroke) قبل التصدير |
| الهامش | اترك ~2 وحدة فراغًا حول الشكل حتى لا يلتصق بالحواف |

### وضعان للعرض

**`mask` (الافتراضي)** — يُستخدم شكل الأيقونة قالبًا ويُلوَّن برمجيًا، فيتغير لونها حسب السياق (أصفر على الخلفية الداكنة، رمادي على الفاتحة، أبيض داخل الأزرار الداكنة).

مهم: هذا الوضع **يتجاهل ألوان الملف تمامًا**. صمّم بلون واحد مصمت. أي تدرّج أو ألوان متعددة ستُسطَّح لشكل واحد.

**`color`** — تُعرض بألوانها الأصلية. للشعار والحالات الفارغة، حيث اللون جزء من الهوية ولا يتغير.

---

## الخانات

### شريط التبويب السفلي — 5 أيقونات

تُعرض بمقاس 17px، وتتلوّن أصفر عند التفعيل ورماديًا عند الخمول. **يجب أن تكون `mask`.**

| الخانة | الشاشة | ملاحظة |
|---|---|---|
| `tab-trip` | رحلتي | نظرة عامة على الرحلة |
| `tab-nights` | الليالي | التقويم |
| `tab-day` | اليوم | المخطط الزمني |
| `tab-places` | أماكني | الأماكن المحفوظة |
| `tab-trips` | رحلاتي | قائمة الرحلات |

### أنواع الأنشطة ووسائل التنقل — 8 أيقونات

تُعرض بمقاس 15px داخل مربّع ملوّن 30×30 في المخطط الزمني، وبمقاس 16px في «أماكني». **`mask`.**

| الخانة | الاستخدام |
|---|---|
| `type-place` | مَعلم أو نشاط سياحي |
| `type-food` | مطعم أو مقهى |
| `type-metro` | مترو |
| `type-train` | قطار |
| `type-shop` | أسواق وتسوّق |
| `type-hotel` | فندق وتسجيل الدخول |
| `type-plane` | طيران |
| `type-car` | سيارة (وتُستخدم للحافلة أيضًا) |

### الأفعال — 8 أيقونات

صغيرة (11–15px)، تظهر داخل الأزرار والروابط. **`mask`.**

| الخانة | الاستخدام |
|---|---|
| `action-add` | إضافة (تذكرة، مدينة، نشاط، مكان، رحلة) |
| `action-edit` | تعديل |
| `action-delete` | حذف عنصر |
| `action-more` | خيارات إضافية |
| `action-copy` | نسخ جدول الحجوزات ومشاركته |
| `action-map` | فتح خرائط جوجل |
| `action-location` | دبوس موقع (الفندق، موقع النشاط) |
| `action-erase` | ممحاة التقويم |

### الهوية والذكاء الاصطناعي — 2

| الخانة | الاستخدام | المقاس | ملاحظة |
|---|---|---|---|
| `brand-logo` | شعار التطبيق في الرأس | 19px | داخل مربّع أصفر. يقبل `color` |
| `ai-sparkle` | كل مواضع الذكاء الاصطناعي | 11–20px | تظهر على خلفية داكنة وفاتحة معًا — يفضّل `mask` |

### الحالات الفارغة — 4

أكبر (22px للأيقونة، أو 92px للأنيميشن). تقبل `color` لأن خلفيتها بيضاء ثابتة. ثلاث منها تعرض أنيميشن حاليًا وتُستخدم الأيقونة بديلًا احتياطيًا.

| الخانة | متى تظهر | الأنيميشن الحالي |
|---|---|---|
| `empty-flights` | لا توجد تذاكر | `airplane` |
| `empty-cities` | لا توجد مدن أو كتل حجز | `booking` |
| `empty-day` | لا يوجد نشاط لهذا اليوم | `car` |
| `empty-places` | لا توجد أماكن محفوظة | — |

### الحالة — 2

| الخانة | الاستخدام |
|---|---|
| `status-warning` | تنبيه الفجوات في التقويم |
| `status-check` | تأكيد ونجاح |

---

## الأولوية

إن أردت البدء بأقل مجهود وأكبر أثر:

**الدفعة الأولى (7)** — أكثر ما يُرى: `tab-trip` · `tab-nights` · `tab-day` · `tab-places` · `tab-trips` · `brand-logo` · `ai-sparkle`

**الدفعة الثانية (8)** — أنواع الأنشطة: كل خانات `type-*`

**الدفعة الثالثة (8)** — الأفعال: كل خانات `action-*`

**الدفعة الرابعة (6)** — الحالات الفارغة والحالة

---

## الأنيميشن (Lottie)

الملفات في `public/assets/lottie/` بصيغة JSON. المتوفر حاليًا: `airplane` · `train` · `car` · `booking` (من حزمة تصميم Tourly، 500×500، 25 إطارًا/ث).

**لإضافة أنيميشن:**

1. نزّل ملف JSON (من [LottieFiles](https://lottiefiles.com) مثلًا) وضعه في `public/assets/lottie/اسمه.json`
2. أضف الاسم إلى `LottieName` في `src/components/Lottie.tsx`
3. استخدمه: `<Lottie name="اسمه" size={92} />`

**قيود تستحق الانتباه:**

- تجنّب الملفات التي تتجاوز 100 كيلوبايت — تبطئ الفتح على شبكات الجوال
- تجنّب الأنيميشن الذي يحتوي صورًا مضمّنة (الأربعة الحالية أشكال خالصة، وهذا هو الأفضل)
- المكتبة تُحمَّل عند أول ظهور حركة فقط، فلا تثقّل التطبيق لمن لا تظهر له
- من فعّل «تقليل الحركة» في نظامه يرى الإطار الأول ساكنًا — هذا مقصود ولا يُعطَّل
